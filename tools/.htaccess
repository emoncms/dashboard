# Apache < 2.4, or 2.4 with mod_access_compat
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
    Satisfy All
</IfModule>

# Apache >= 2.4
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
